fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
# Formularios del dashboard (editor de guion). Sin él, FastAPI revienta al
# IMPORTAR las rutas y la app entera no arranca, no solo el editor.
python-multipart>=0.0.9
apscheduler>=3.10,<4
requests>=2.32
feedparser>=6.0
python-dotenv>=1.0
pyyaml>=6.0
